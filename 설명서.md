# 📷 나와 어울리는 필름카메라 찾기 — 설명서

12개의 질문에 답하면 당신의 촬영 성향을 분석해 가장 잘 맞는 필름카메라를 추천하는 심리테스트 웹앱입니다.

---

## 빠른 시작

가장 간단한 방법은 `film-camera-test.html` 파일 하나만 있으면 됩니다.

1. `film-camera-test.html`을 다운로드합니다.
2. 파일을 더블클릭하거나 브라우저(Chrome, Safari, Edge 등)로 엽니다.
3. 끝. 인터넷 연결이나 설치 없이 바로 실행됩니다.

카메라 사진이 파일 안에 직접 내장되어 있어서, 어디서 열든 이미지가 항상 보입니다. 이메일로 보내거나 USB에 담아도 그대로 작동합니다.

---

## 어떻게 작동하나요

### 1. 질문 (12문항)
촬영 철학과 라이프스타일을 묻는 12개의 질문이 나옵니다. 각 질문마다 3개의 선택지 중 하나를 고릅니다.

- 왼쪽 선택지 — 한쪽 성향
- 상관없음 — 중립
- 오른쪽 선택지 — 반대 성향

### 2. 성향 분석 (5개 축)
답변은 다음 5가지 축으로 점수화됩니다.

| 축 | 의미 |
|----|------|
| Speed | 즉흥적 ↔ 계획적 |
| Control | 자동 ↔ 수동 |
| Emotion | 감성 ↔ 기술 |
| Simplicity | 간단함 ↔ 정교함 |
| Perfectionism | 자유로움 ↔ 완벽주의 |

### 3. 결과
- **성향 맵** — 가로축(빠름)과 세로축(완벽성)의 2D 좌표에 당신의 위치가 노란 별(★)로 표시됩니다. 청록색 점이 추천 카메라, 회색 점이 나머지 카메라입니다.
- **추천 카메라 3개** — 당신과 성향 방향이 가장 비슷한 카메라 3대를 최고의 매치 순으로 보여줍니다.
- **정반대 스타일 카메라 1개** — 완전히 다른 촬영 철학을 경험해보고 싶은 사람을 위한 반대 성향 카메라입니다.
- **다시 시작** — 처음부터 다시 테스트할 수 있습니다.

> 답변 중 **← 이전 질문** 버튼으로 언제든 앞 질문으로 돌아가 답을 바꿀 수 있습니다.

---

## 수록된 카메라 (12종)

| 카메라 | 캐릭터 |
|--------|--------|
| Hasselblad 500C/M | 완벽주의자 |
| Leica M6 | 우아한 신사 |
| Nikon FM2 | 성실한 노동자 |
| Pentax K1000 | 소박한 스승 |
| Yashica Electro 35 | 친근한 동반자 |
| Holga | 장난꾸러기 |
| Mamiya RB67 | 실용적 거장 |
| Olympus Mju II | 매력적인 스트릿 뮤즈 |
| Pentax 67 | 영화 같은 몽상가 |
| Canon AE-1 | 혁신가 |
| Contax T2 | 세련된 미니멀리스트 |
| Rolleiflex | 클래식 우아함 |

각 카메라는 성격 설명, 한 줄 요약, 상세 설명, 강점 정보를 함께 제공합니다.

---

## 주요 기능

- **모바일 최적화** — 휴대폰에서도 편하게 답할 수 있는 큰 버튼과 글씨
- **반응형 레이아웃** — 넓은 화면에서는 추천 카드가 2개씩 나란히, 좁은 화면에서는 1개씩 세로로 배치
- **실제 카메라 사진** — 각 결과에 실제 제품 사진 표시
- **다크 테마** — 눈이 편한 어두운 배경의 세련된 디자인
- **결과 공유** — 링크 복사로 결과를 친구에게 전달
- **다시 시작** — 언제든 처음부터 다시 테스트 가능

---

## 파일 구성

### 그냥 쓰고 싶다면
- **`film-camera-test.html`** — 이 파일 하나면 끝입니다. 브라우저로 바로 여세요.

### 직접 수정하거나 React로 개발하고 싶다면
| 파일 | 역할 |
|------|------|
| `src-App.js` | 앱의 메인 로직 (질문, 카메라 데이터, 점수 계산) |
| `src-App.css` | 스타일 (다크 테마, 반응형) |
| `src-index.js` | React 진입점 |
| `src-index.css` | 전역 스타일 |
| `public-index.html` | HTML 템플릿 |
| `package.json` | 의존성 정의 |

React 버전 실행 방법:

```bash
npm install
npm start        # 개발 서버 실행
npm run build    # 배포용 빌드
```

> 참고: React 버전은 카메라 이미지를 별도로 연결해야 합니다. 사진이 내장된 완성본은 `film-camera-test.html`입니다.

---

## 내용 수정하기

`film-camera-test.html`을 텍스트 편집기로 열면 아래 항목을 직접 바꿀 수 있습니다.

- **질문 바꾸기** — `const questions = [` 부분에서 질문 문구와 선택지 수정
- **카메라 정보 바꾸기** — `const cameras = [` 부분에서 설명, 강점, 점수 수정
- **색상 바꾸기** — 파일 상단 `:root` 안의 색상 변수 수정

각 카메라의 점수(`scores`)를 바꾸면 추천 결과와 맵 위치가 달라집니다.

---

## 자주 묻는 질문

**Q. 인터넷이 없어도 되나요?**
네. 모든 것이 HTML 파일 안에 들어있어 오프라인에서도 완전히 작동합니다.

**Q. 휴대폰에서도 되나요?**
네. 모바일에 맞춰 디자인되어 있습니다. 파일을 휴대폰으로 옮겨 브라우저로 열면 됩니다.

**Q. 결과가 매번 똑같나요?**
답변이 같으면 결과도 같습니다. 당신의 선택에 가장 가까운 카메라를 계산하는 방식이라 일관적입니다.

**Q. 카메라를 추가하고 싶어요.**
`const cameras = [` 목록에 새 항목을 추가하고, 5개 축 점수를 넣으면 됩니다. 다만 사진은 별도로 넣어야 합니다.
